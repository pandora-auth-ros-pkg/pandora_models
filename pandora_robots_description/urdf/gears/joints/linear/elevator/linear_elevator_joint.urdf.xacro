<?xml version="1.0" ?>

<robot xmlns:xacro="http://ros.org/wiki/xacro" >

<!-- ====================================================================== -->
<xacro:include filename="$(find pandora_robots_description)/urdf/gears/joints/linear/elevator/linear_elevator_joint_config.xacro" />
<!-- ====================================================================== -->

<!-- ###################################################################### -->

  <xacro:macro name="linear_elevator_joint"
               params="fixed" >

    <!-- ================================================================== -->
    <xacro:if value="${fixed}" >
    
      <joint name="linear_elevator_joint"
             type="fixed" >

        <parent link="base_link" />
        <child link="linear_elevator_link" />

        <xacro:linear_elevator_joint_origin />

      </joint>
      
    </xacro:if>
    <!-- ================================================================== -->
    <xacro:unless value="${fixed}" >
    
      <joint name="linear_elevator_joint"
             type="${linear_elevator_joint_type}" >

        <parent link="base_link" />
        <child link="linear_elevator_link" />

        <xacro:linear_elevator_joint_origin />

        <xacro:linear_elevator_joint_physics />

      </joint>
      
    </xacro:unless>
    <!-- ================================================================== -->
    
  </xacro:macro>

<!-- ###################################################################### -->

</robot>
